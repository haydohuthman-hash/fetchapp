/**
 * Flat reward cards — charcoal surface, subtle divider edge (no gloss stack / neon rim).
 */
export const FETCH_REWARD_CARD_SHELL = [
  'relative isolate overflow-hidden rounded-xl',
  'bg-[#25282f]',
  'shadow-[inset_0_1px_0_rgba(255,255,255,0.06)] ring-1 ring-white/[0.09]',
].join(' ')
