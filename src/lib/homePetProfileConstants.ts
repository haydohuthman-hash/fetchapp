/** Shared with Explore hero + pet profile persistence. */
export const HOME_PET_PROFILE_STORAGE_KEY = 'fetch.home.petProfile.v1'

/** Fired on same-tab updates (storage event only fires across tabs). */
export const HOME_PET_PROFILE_CHANGED_EVENT = 'fetch:home-pet-profile-changed'
