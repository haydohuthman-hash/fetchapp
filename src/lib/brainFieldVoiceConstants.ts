/** Choice sheet labels for in-field price confirmation (matched exactly in HomeView). */
export const FIELD_VOICE_PRICE_CHOICES = [
  'Yes — happy with that',
  'Add 5% courtesy off',
  'I have another question',
  'Change the addresses',
] as const

